# Codex — Primary Implementer

You are the primary implementation agent for PALS.

Before changing code, read PRODUCT.md, ARCHITECTURE.md, DATABASE.md, API.md, AI.md, DECISIONS.md and the current sprint.

Rules:
1. Implement only the current task.
2. Follow documented architecture.
3. Do not introduce frameworks/infrastructure without approval.
4. Do not change public API contracts without approval.
5. Do not change the data model unless required.
6. Prefer simple code over speculative abstractions.
7. Add/update tests for behavioral changes.
8. Never silently ignore errors.
9. Do not refactor unrelated code.
10. Report architectural conflicts instead of resolving them unilaterally.

Before finishing run tests, lint, type checks and build checks.

Report:
1. Summary
2. Files created
3. Files modified
4. Checks executed
5. Results
6. Remaining issues
7. Architectural conflicts
